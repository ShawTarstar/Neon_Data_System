<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.taxing.tliaswebmanagement.mapper.mysql.ClazzMapper">
    <insert id="insert">
        insert into clazz(name,room,begin_date,end_date,master_id,subject,create_time,update_time)
        values (#{name},#{room},#{beginDate},#{endDate},#{masterId},#{subject},#{createTime},#{updateTime})
    </insert>
    <update id="update">
        update clazz set name=#{name},room=#{room},
                         begin_date=#{beginDate},end_date=#{endDate},
                         master_id=#{masterId},subject=#{subject},
                         update_time=#{updateTime}
        where id=#{id}
    </update>
    <delete id="delete">
        delete from clazz where id=#{id}
    </delete>

    <select id="list" resultType="com.taxing.tliaswebmanagement.pojo.Clazz">
        select c.id,c.name,c.room,c.begin_date,
               c.end_date,c.master_id,e.name master_name,
               c.create_time,c.update_time 
        from clazz c left join emp e on c.master_id=e.id
        <where>
            <if test="name!='' and name!=null">
                c.name like concat('%',#{name},'%')
            </if>
            <if test="begin!=null and end!=null">
                and c.end_date between #{begin} and #{end}
            </if>
        </where>

    </select>
    <select id="selectById" resultType="com.taxing.tliaswebmanagement.pojo.Clazz">
        select id,name,room,begin_date,end_date,master_id,subject,create_time,update_time
        from clazz where id=#{id}
    </select>
    <select id="selectInStu" resultType="java.lang.Integer">
        select count(*) from student where clazz_id=#{id}
    </select>
    <select id="selectAllClazzs" resultType="com.taxing.tliaswebmanagement.pojo.Clazz">
        select id,name,room,begin_date,end_date,master_id,subject,create_time,update_time from clazz
    </select>
</mapper>