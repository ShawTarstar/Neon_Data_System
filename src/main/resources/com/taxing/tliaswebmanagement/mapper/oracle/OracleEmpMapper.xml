<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.taxing.tliaswebmanagement.mapper.oracle.OracleEmpMapper">

    <!-- ====================== update ====================== -->
    <update id="updateById">
        UPDATE "emp"
        <set>
            <if test="username != null and username!=''">
                "username" = #{username},
            </if>
            <if test="password != null and password!=''">
                "password" = #{password},
            </if>
            <if test="name != null and name!=''">
                "name" = #{name},
            </if>
            <if test="gender != null">
                "gender" = #{gender},
            </if>
            <if test="phone != null and phone!=''">
                "phone" = #{phone},
            </if>
            <if test="job != null">
                "job" = #{job},
            </if>
            <if test="salary != null">
                "salary" = #{salary},
            </if>
            <if test="image != null and image!=''">
                "image" = #{image},
            </if>
            <if test="entryDate != null">
                "entry_date" = #{entryDate},
            </if>
            <if test="deptId != null">
                "dept_id" = #{deptId},
            </if>
            <if test="updateTime!=null">
                "update_time" = #{updateTime}
            </if>
        </set>
        WHERE "id" = #{id}
    </update>

    <!-- ====================== delete ====================== -->
    <delete id="deleteByIds">
        DELETE FROM "emp" WHERE "id" IN
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>

    <!-- ====================== list ====================== -->
    <select id="list" resultType="com.taxing.tliaswebmanagement.pojo.Emp">
        SELECT
        e."id",
        e."username",
        e."password",
        e."name",
        e."gender",
        e."phone",
        e."job",
        e."salary",
        e."image",
        e."entry_date",
        e."dept_id",
        e."create_time",
        e."update_time",
        d."name" AS "deptName"
        FROM "emp" e
        LEFT JOIN "dept" d ON e."dept_id" = d."id"
        <where>
            <if test="name!=null and name!=''">
                e."name" LIKE '%' || #{name} || '%'
            </if>
            <if test="gender!=null">
                AND e."gender" = #{gender}
            </if>
            <if test="begin!=null and end!=null">
                AND e."entry_date" BETWEEN #{begin} AND #{end}
            </if>
        </where>
        ORDER BY e."update_time" DESC
    </select>

    <!-- ====================== resultMap ====================== -->
    <resultMap id="empResultMap" type="com.taxing.tliaswebmanagement.pojo.Emp">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="name" property="name"/>
        <result column="gender" property="gender"/>
        <result column="phone" property="phone"/>
        <result column="job" property="job"/>
        <result column="salary" property="salary"/>
        <result column="image" property="image"/>
        <result column="entry_date" property="entryDate"/>
        <result column="dept_id" property="deptId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>

        <collection property="exprList" ofType="com.taxing.tliaswebmanagement.pojo.EmpExpr">
            <id column="ee_id" property="id"/>
            <result column="ee_empid" property="empId"/>
            <result column="ee_begin" property="begin"/>
            <result column="ee_end" property="end"/>
            <result column="ee_company" property="company"/>
            <result column="ee_job" property="job"/>
        </collection>
    </resultMap>

    <!-- ====================== getById ====================== -->
    <select id="getById" resultMap="empResultMap">
        SELECT
            e."id",
            e."username",
            e."password",
            e."name",
            e."gender",
            e."phone",
            e."job",
            e."salary",
            e."image",
            e."entry_date",
            e."dept_id",
            e."create_time",
            e."update_time",
            ee."id" AS "ee_id",
            ee."emp_id" AS "ee_empid",
            ee."begin" AS "ee_begin",
            ee."end" AS "ee_end",
            ee."company" AS "ee_company",
            ee."job" AS "ee_job"
        FROM "emp" e
                 LEFT JOIN "emp_expr" ee ON e."id" = ee."emp_id"
        WHERE e."id" = #{id}
    </select>

    <!-- ====================== countEmpJobData ====================== -->
    <select id="countEmpJobData" resultType="java.util.Map">
        SELECT
            CASE
                WHEN "job" = 1 THEN '班主任'
                WHEN "job" = 2 THEN '讲师'
                WHEN "job" = 3 THEN '学工主管'
                WHEN "job" = 4 THEN '教研主管'
                WHEN "job" = 5 THEN '咨询师'
                ELSE '其他'
                END AS "pos",
            COUNT(*) AS "num"
        FROM "emp"
        GROUP BY "job"
        ORDER BY "num"
    </select>

    <!-- ====================== countEmpGenderData ====================== -->
    <select id="countEmpGenderData" resultType="java.util.Map">
        SELECT
            CASE WHEN "gender" = 1 THEN '男性员工' ELSE '女性员工' END AS "name",
            COUNT(*) AS "value"
        FROM "emp"
        GROUP BY "gender"
    </select>

    <!-- ====================== select ====================== -->
    <select id="select" resultType="com.taxing.tliaswebmanagement.pojo.EmpDTO">
        SELECT
            "id",
            "username",
            "password",
            "name",
            "gender",
            "image",
            "job",
            "salary",
            "entry_date",
            "dept_id",
            "create_time",
            "update_time"
        FROM "emp"
    </select>

</mapper>
